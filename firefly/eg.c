#include<stdio.h>
#include<math.h>
main()
{
    printf("%f",exp(sqrt(2)));
}


    double rval(int i,int j)
{
    int k;
    double sum,r;
    for(k=0;k<10;k++)
    sum+=(d[i].posval[k]-d[j].posval[k])*(d[i].posval[k]-d[j].posval[k]);

    r=sqrt(sum);
    return r;
}
